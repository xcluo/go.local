from setuptools import setup, find_packages

setup(
    name="test_celery",
    version="3.0.0",
    description='DB Backends for online learning',
    author='Simon Luo',

    packages=find_packages(),
    keywords=('test_celery', 'python27', 'django17', 'django_model'),
)
